# AWS Single EC2 Instance with Terraform Module

This Terraform project creates a reusable module to launch a single EC2 instance.

## 🔧 How to Use

1. Clone the repository
2. Customize `terraform.tfvars` or use CLI variables
3. Run:

```bash
terraform init
terraform plan
terraform apply
